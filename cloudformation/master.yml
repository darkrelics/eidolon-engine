AWSTemplateFormatVersion: "2010-09-09"
Description: "Master template for Eidolon Engine infrastructure"

Parameters:
  GameName:
    Type: String
    Default: "eidolon"
    Description: "Name of the game"
  ReplyEmailAddress:
    Type: String
    Description: "Email address for Cognito notifications"
    Default: "noreply@example.com"
  LambdaBucketName:
    Type: String
    Description: "S3 bucket containing Lambda deployment packages and CloudFormation templates"
  LambdaZipKey:
    Type: String
    Default: "lambda.zip"
    Description: "S3 key for Lambda deployment package"
  LayerZipKey:
    Type: String
    Default: "layer.zip"
    Description: "S3 key for Lambda layer package"
  PortalDomainName:
    Type: String
    Default: ""
    Description: "Custom domain for portal (optional)"
  ApiDomainName:
    Type: String
    Default: ""
    Description: "Custom domain for API (optional)"
  CertificateArn:
    Type: String
    Default: ""
    Description: "ACM certificate ARN for custom domains (optional)"
  CloudWatchRetentionDays:
    Type: Number
    Default: 365
    Description: "CloudWatch log retention in days"

Resources:
  # CloudWatch logging (created first for policies)
  CloudWatchStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/cloudwatch.yml"
      Parameters:
        GameName: !Ref GameName
        RetentionInDays: !Ref CloudWatchRetentionDays
      Tags:
        - Key: Component
          Value: CloudWatch

  # DynamoDB tables (created first for policies)
  DynamoDBStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/dynamo.yml"
      Parameters:
        GameName: !Ref GameName
      Tags:
        - Key: Component
          Value: DynamoDB

  # IAM roles with attached policies
  IAMStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/iam.yml"
      Parameters:
        GameName: !Ref GameName
        DynamoDBAccessPolicyArn: !GetAtt DynamoDBStack.Outputs.DynamoDBAccessPolicyArn
        CloudWatchPolicyArn: !GetAtt CloudWatchStack.Outputs.CloudWatchPolicyArn
      Tags:
        - Key: Component
          Value: IAM

  # S3 buckets
  S3Stack:
    Type: AWS::CloudFormation::Stack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/s3.yml"
      Parameters:
        GameName: !Ref GameName
      Tags:
        - Key: Component
          Value: S3

  # Cognito with Lambda triggers
  CognitoStack:
    Type: AWS::CloudFormation::Stack
    DependsOn:
      - IAMStack
      - S3Stack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/cognito.yml"
      Parameters:
        GameName: !Ref GameName
        ReplyEmailAddress: !Ref ReplyEmailAddress
        LambdaBucketName: !Ref LambdaBucketName
        LambdaZipKey: !Ref LambdaZipKey
      Tags:
        - Key: Component
          Value: Cognito

  # Lambda functions and API Gateway
  LambdaStack:
    Type: AWS::CloudFormation::Stack
    DependsOn:
      - IAMStack
      - DynamoDBStack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/lambda.yml"
      Parameters:
        GameName: !Ref GameName
        LambdaBucketName: !Ref LambdaBucketName
        LambdaZipKey: !Ref LambdaZipKey
        LayerZipKey: !Ref LayerZipKey
        CognitoUserPoolId: !GetAtt CognitoStack.Outputs.UserPoolId
        DomainName: !Ref ApiDomainName
        CertificateArn: !Ref CertificateArn
      Tags:
        - Key: Component
          Value: Lambda

  # CloudFront distribution
  CloudFrontStack:
    Type: AWS::CloudFormation::Stack
    DependsOn:
      - S3Stack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/cloudfront.yml"
      Parameters:
        GameName: !Ref GameName
        DomainName: !Ref PortalDomainName
        CertificateArn: !Ref CertificateArn
      Tags:
        - Key: Component
          Value: CloudFront

  # CodeBuild for deployments
  CodeBuildStack:
    Type: AWS::CloudFormation::Stack
    DependsOn:
      - S3Stack
    Properties:
      TemplateURL: !Sub "https://s3.amazonaws.com/${LambdaBucketName}/cloudformation/codebuild.yml"
      Parameters:
        GameName: !Ref GameName
        CognitoUserPoolId: !GetAtt CognitoStack.Outputs.UserPoolId
        CognitoUserPoolClientId: !GetAtt CognitoStack.Outputs.UserPoolClientId
        CognitoIdentityPoolId: !GetAtt CognitoStack.Outputs.IdentityPoolId
        ApiUrl: !GetAtt LambdaStack.Outputs.ApiGatewayUrl
      Tags:
        - Key: Component
          Value: CodeBuild

Outputs:
  # S3 Buckets
  PortalBucketName:
    Description: "S3 bucket for portal files"
    Value: !GetAtt S3Stack.Outputs.PortalBucketName

  ScriptsBucketName:
    Description: "S3 bucket for scripts"
    Value: !GetAtt S3Stack.Outputs.ScriptsBucketName

  LambdaBucketName:
    Description: "S3 bucket for Lambda packages"
    Value: !GetAtt S3Stack.Outputs.LambdaBucketName

  # CloudFront
  CloudFrontDistributionId:
    Description: "CloudFront distribution ID"
    Value: !GetAtt CloudFrontStack.Outputs.CloudFrontDistributionId

  CloudFrontDistributionDomainName:
    Description: "CloudFront distribution domain"
    Value: !GetAtt CloudFrontStack.Outputs.CloudFrontDistributionDomainName

  # API Gateway
  ApiGatewayUrl:
    Description: "API Gateway URL"
    Value: !GetAtt LambdaStack.Outputs.ApiGatewayUrl

  # Cognito
  UserPoolId:
    Description: "Cognito User Pool ID"
    Value: !GetAtt CognitoStack.Outputs.UserPoolId

  UserPoolClientId:
    Description: "Cognito User Pool Client ID"
    Value: !GetAtt CognitoStack.Outputs.UserPoolClientId

  IdentityPoolId:
    Description: "Cognito Identity Pool ID"
    Value: !GetAtt CognitoStack.Outputs.IdentityPoolId

  # DynamoDB Tables
  PlayersTableName:
    Description: "Players table name"
    Value: !GetAtt DynamoDBStack.Outputs.PlayersTableName

  CharactersTableName:
    Description: "Characters table name"
    Value: !GetAtt DynamoDBStack.Outputs.CharactersTableName

  StoryTableName:
    Description: "Story table name"
    Value: !GetAtt DynamoDBStack.Outputs.StoryTableName

  # IAM Roles
  ServerExecutionRoleArn:
    Description: "Server execution role ARN"
    Value: !GetAtt IAMStack.Outputs.ServerExecutionRoleArn

  LambdaExecutionRoleArn:
    Description: "Lambda execution role ARN"
    Value: !GetAtt IAMStack.Outputs.LambdaExecutionRoleArn

  # CloudWatch
  LogGroupName:
    Description: "CloudWatch log group name"
    Value: !GetAtt CloudWatchStack.Outputs.LogGroupName

  # CodeBuild
  CodeBuildProjectName:
    Description: "CodeBuild project name"
    Value: !GetAtt CodeBuildStack.Outputs.CodeBuildProjectName
