name: cognito_email_verification
description: Eidolon Engine email verification app.

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  amazon_cognito_identity_dart_2: ^3.6.5
  http: ^1.2.2
  crypto: ^3.0.3
  provider: ^6.1.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
